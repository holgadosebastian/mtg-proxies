import React from "react"

const AddNewCard = () => <button id="AddNewCard">Add New</button>

export const CardList = () => (
  <div id="CardList">
    <div className="card" />
    <AddNewCard />
  </div>
)
